<%= form_with(model: post, local: true) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :references %>
    <%= form.text_field :references, id: :post_references %>
  </div>

  <div class="field">
    <%= form.label :string %>
    <%= form.text_field :string, id: :post_string %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
